from app.word_agent.active_word_writer import continue_active_document

result = continue_active_document(
    "continue this cybersecurity report and add sections about cloud security, zero trust, and future trends"
)

print(result)